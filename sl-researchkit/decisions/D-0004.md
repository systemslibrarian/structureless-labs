Decision ID: D-0004 (sl-researchkit)
Question:     Should the `decryption-failure` atlas concept — BLOCKED by
              D-0003 on 2026-06-09 for a missing Simple view — be published
              now that the Simple view has been authored and re-reviewed?
Alternatives: (A) Publish: the Teacher re-review returned PASS.
              (B) Keep holding: require a second reviewer pass or a waiting
                  period before resolving the lab's first BLOCK.
Chosen:       **(A)** — Publish. The BLOCK is resolved, not overridden.
Reason:       D-0003 specified the resolution path explicitly: author the
              Simple view to the standard of the 2026-06-09 Teacher review,
              re-enter the gate, and record a superseding decision if the
              fresh review PASSes. Every step was followed: the Simple view
              was authored (2026-07-11), the fresh Teacher review
              (`sl-atlas/reviews/TEACHER-2026-07-11-decryption-failure.md`)
              checked it against the original review's named requirements
              — including the Attacker persona's carried note that the
              Simple view must not anchor an exponent the Researcher view
              does not bind — and returned PASS. Option B would invent a
              new gate D-0003 never asked for, which is its own kind of
              contract-breaking.
Risks:        Resolving the first BLOCK could be read as the gate softening.
              Mitigated by the record itself: the BLOCK stood for 32 days,
              publication happened only after the named deficiency was
              fixed, and both the BLOCK and the resolution are permanent
              artifacts. A gate that can never be satisfied is not a gate;
              it is a wall.
Evidence:     Grade D (governance decision, not measured).
Reviewed by:  Teacher — VERDICT: PASS (dispositive; see the 2026-07-11
                re-review).
              Archivist — VERDICT: PASS. D-0003 is superseded, not edited;
                the BLOCK record is preserved verbatim per
                never-edit-destructively.
              Principal Investigator — VERDICT: PASS. The research question
                "can a BLOCK be resolved through the specified path?" now
                has a YES on the record, completing the gate's lifecycle
                demonstration (block → hold → resolve).
Date:         2026-07-11
Status:       Active — supersedes the publication-hold in D-0003
              (sl-researchkit). D-0003 remains on the record unchanged.

Outcome:
- `decryption-failure` is published: it appears in `sl-atlas/src/concepts.js`
  (generated from `sl-atlas/content/decryption-failure.json`, whose BLOCKED
  status field is removed) at learning-path position 7.
- `sl-atlas/src/blocked.js` is now an empty list; the sidebar's Teacher-gate
  section renders an explicit "empty" state pointing at the decision log,
  because "nothing is currently blocked" is itself information.
- The atlas count toward PREDICTION-002 now includes this concept.

---
*Soli Deo Gloria — 1 Corinthians 10:31*
