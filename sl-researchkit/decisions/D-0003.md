Decision ID: D-0003 (sl-researchkit)
Question:     Should the proposed `decryption-failure` atlas concept be
              published with two of three depths authored (Developer +
              Researcher) and the Simple view marked `null`?
Alternatives: (A) Publish as-is; mark Simple view as TODO; flag in the UI.
              (B) Publish with a placeholder Simple view written as a brief
                  rewording of the Developer view.
              (C) Do NOT publish. Hold the draft in `sl-atlas/content/` with
                  a BLOCKED status flag; concept does not enter
                  `sl-atlas/src/concepts.js` until the Simple view is
                  authored to the standard the Teacher gate requires.
Chosen:       **(C)** — Do not publish. The BLOCK stands.
Reason:       Articles 5 of `sl-kem/CONSTITUTION.md` ("Explainability: every
              concept ships with Simple / Developer / Researcher views") and
              the Teacher persona's mandate are not negotiable on volume
              days. If they were, they would not be gates. Option A would
              silently weaken the contract; Option B would launder the
              Developer view into a Simple-view-shaped placeholder, which
              the carried-forward "Simple view comprehensible without the
              diagram" question from D-0001's Teacher review explicitly
              warned against.
Risks:        Holding a draft creates the impression of incompleteness on a
              tour of the repo. Mitigated by an explicit BLOCKED stub in the
              sidebar roadmap that links to this decision and the Teacher
              review, so the BLOCK itself is visible — the user can see the
              gate at work.
Evidence:     Grade D (governance / framing decision, not measured).
Reviewed by:  Teacher — **VERDICT: BLOCK** (see
                `sl-atlas/reviews/TEACHER-2026-06-09-decryption-failure.md`).
                This is the dispositive verdict.
              Principal Investigator — VERDICT: PASS (on Option C). The
                research question advanced is "do the gates produce
                different outcomes on different submissions?", and Option C
                produces the first NO outcome on the record.
              Archivist — VERDICT: PASS. The BLOCKED draft is preserved in
                content/ (not deleted), which honors the permanence
                principle and lets a future author resolve the BLOCK by
                authoring the missing depth rather than starting over.
              Cryptographer — VERDICT: PASS. The Researcher view is
                technically correct; the BLOCK is purely about explainability,
                not correctness. No security claim is being suppressed.
              Attacker — VERDICT: PASS WITH NOTES. The Developer view's
                "modern KEMs target `≤ 2⁻¹⁶⁰`" sentence is approximately
                correct but the specific exponent varies across schemes;
                whoever writes the Simple view should not anchor a number
                that the Researcher view does not bind. Note carried to the
                resubmission.
              Engineer — VERDICT: PASS. No implementation impact.
Date:         2026-06-09
Status:       Active

Outcome (what the BLOCK actually changes about the repo):
- `decryption-failure` is **NOT** added to `sl-atlas/src/concepts.js` — so it
  does NOT appear as a published concept in the live atlas.
- `sl-atlas/content/decryption-failure.json` keeps the draft (Developer +
  Researcher views authored honestly; Simple view explicitly `null`) so the
  prior work is not lost.
- A new BLOCKED stub appears in the atlas sidebar so the gate's outcome is
  visible to readers, not hidden in a review file.
- The atlas count toward `PREDICTION-002` stays at **6/12**, not 7/12. The
  BLOCKED draft does NOT contribute to the prediction's target.

Provenance note:
Before this decision, every persona review on the record had returned PASS
or PASS-WITH-NOTES (resolved-in-commit). The lab's own caveat — surfaced in
the Reconciliation Teacher review as a forward question — was that three
PASSes in one day looked like it could become rubber-stamping. This decision
is the dispositive answer to that question: the gates produce different
outcomes on different submissions; one of them is BLOCK; the BLOCK actually
prevents publication. The carried-forward "review pacing" question remains
open for next month, but the gate's basic teeth are now demonstrated rather
than asserted.

Resolution path (for a future author):
1. Read `sl-atlas/reviews/TEACHER-2026-06-09-decryption-failure.md`.
2. Author the Simple view to the standard described in section "What must
   change before this can re-enter the gate."
3. Open a fresh Teacher review (this decision's BLOCK does not pre-decide
   the next review's verdict).
4. If the next review PASSes, add the entry to `sl-atlas/src/concepts.js`,
   strike through this BLOCKED stub in the roadmap, and record a new
   decision superseding this one (per the never-edit-destructively rule).

---
*Soli Deo Gloria — 1 Corinthians 10:31*
